# API

No API endpoints exist yet. `backend/config/urls.py` currently only routes `/admin/`.

`agent_api` (`backend/agent_api/`) is scaffolded but empty — no models, no views, not registered in `INSTALLED_APPS`. Once endpoints are added, document them here: method, path, request/response shape, auth requirements.
